# Supabase Project Credentials
# Get these from your Supabase project's API settings
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Database Connection String
# Get this from your Supabase project's Database settings
# Important: For Prisma Migrate, use a connection string with a password that is not pooled.
# e.g., postgresql://postgres:[YOUR-PASSWORD]@[YOUR-HOST]:5432/postgres
# For the application itself (directUrl), you can use the pooled connection string.
DATABASE_URL=
DIRECT_URL=

# Secret key for NextAuth.js or other session management.
# Generate a strong secret with: openssl rand -base64 32
NEXTAUTH_SECRET=

# Secret key for encrypting sensitive data like employee PINs.
# Generate a strong secret with: openssl rand -base64 32
ENCRYPTION_KEY=

# Secret for signing webhook payloads sent from this application.
# Generate a strong secret with: openssl rand -base64 32
WEBHOOK_SIGNING_SECRET=
